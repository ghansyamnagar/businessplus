// Type Imports
import type { ThemeColor } from '@core/types'

export type UnitsType = {
  id: number
  unit_name: string
  unit_address: string
  company_name: string
  enable: string // 'Yes' | 'No' or similar, based on data
}
