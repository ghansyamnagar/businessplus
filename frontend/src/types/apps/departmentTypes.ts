
// Type Imports
import type { ThemeColor } from '@core/types'

export type DepartmentType = {
    dept_id: number
    dept_name: string
    user_id: string
    name: string
    unit_id: number
    unit_name: string
    email: string
    role_id?: string
    company_id?: number
}
